
#include "main.h"
#include "global.h"
/*
void stopDriving(){
    motorSet(BL, 0);
    motorSet(TL, 0);
    motorSet(TR, 0);
    motorSet(BR, 0);
}
void forward(){
  motorSet(TL, 127);
  motorSet(BL, 127);
  motorSet(TR, -127);
  motorSet(BR, -127);
}


void backward(){
  motorSet(TL, -127);
  motorSet(BL, -127);
  motorSet(TR, 127);
  motorSet(BR, 127);
}
void right(){
  motorSet(TL, 127);
  motorSet(BL, 127);
  motorSet(TR, 127);
  motorSet(BR, 127);
}
void left(){
  motorSet(TL, -127);
  motorSet(BL, -127);
  motorSet(TR, -127);
  motorSet(BR, -127);
}
*/
void autonomous() {
  /*
forward();
delay(2000);
stopDriving();
*/
}
