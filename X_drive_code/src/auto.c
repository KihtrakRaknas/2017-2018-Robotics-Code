
#include "main.h"
#include "global.h"

/*
void forward(int time){
            motorSet(TL, 127);
            motorSet(BR, -127);
            motorSet(TR, -127);
            motorSet(BL, 127);
	delay(time);
	  motorStop(TL);
            motorStop(BR);
            motorStop(TR);
            motorStop(BL);
}

void backward(int time){
            motorSet(TL, -127);
            motorSet(BR, 127);
            motorSet(TR, 127);
            motorSet(BL, -127);
delay(time);
	  motorStop(TL);
            motorStop(BR);
            motorStop(TR);
            motorStop(BL);
}

void right(int time){
            motorSet(TL, 127);
            motorSet(BR, -127);
            motorSet(TR, 127);
            motorSet(BL, -127);
delay(time);
	  motorStop(TL);
            motorStop(BR);
            motorStop(TR);
            motorStop(BL);
}
void left(int time){
            motorSet(TL, -127);
            motorSet(BR, 127);
            motorSet(TR, -127);
            motorSet(BL, 127);
	delay(time);
	  motorStop(TL);
            motorStop(BR);
            motorStop(TR);
            motorStop(BL);
}
*/
void autonomous() {
  /*
	forward(5000);
	left(2500);
	right(5000);
	left(2500);
	backward(5000);
  */
}
